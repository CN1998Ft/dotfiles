#!/usr/bin/env bash

sketchybar --set $NAME label="$FOCUSED_WORKSPACE"
# if [[ "$1" == "$FOCUSED_WORKSPACE" ]]; then
# 	sketchybar --set $NAME label=""
# else
# 	sketchybar --set $NAME label=""
# fi
